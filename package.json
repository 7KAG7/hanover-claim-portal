{
  "name": "hanover-claim-portal",
  "private": true,
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "type": "module",
  "scripts": {
    "dev:api": "pnpm --filter api dev",
    "dev:react": "pnpm --filter react-ui dev",
    "build": "pnpm -r build",
    "start:api": "pnpm --filter api start",
    "prisma": "pnpm --filter api prisma"
  }
}
